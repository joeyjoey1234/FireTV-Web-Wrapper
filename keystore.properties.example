# Copy this file to keystore.properties (don't commit secrets!) and replace the placeholder values.
# The release signing key must always use the alias "joepe-na-signer".
storeFile=/absolute/path/to/firetvwebwrapper.keystore
storePassword=CHANGE_ME
keyAlias=joepe-na-signer
keyPassword=CHANGE_ME
